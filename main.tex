% Not All Errors Are Equal: Consequence-Aware Reasoning Compute Allocation
% Target: ICLR 2027 (deadline ~Sept-Oct 2026)

\documentclass{article}
\usepackage{iclr2026_conference,times}

\input{math_commands.tex}

\usepackage[hidelinks,colorlinks=false]{hyperref}
\usepackage{url}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{booktabs}
\title{Not All Errors Are Equal:\\Consequence-Aware Reasoning Compute Allocation}

% The \author macro works with any number of authors. Use \And or \AND
% to break the author list across columns; we go anonymous for submission.
\author{Anonymous authors\\
Paper under double-blind review}

\newcommand{\fix}{\marginpar{FIX}}
\newcommand{\new}{\marginpar{NEW}}

\iclrfinalcopy % Uncomment for camera-ready

\begin{document}

\maketitle

\begin{abstract}
% Modern reasoning models---OpenAI's o-series, DeepSeek-R1, Claude with
% extended thinking, and Qwen3-Thinking---allocate test-time compute
% adaptively across tasks: tasks that look harder receive more thinking
% tokens, easier ones stop earlier. Recent work on adaptive test-time
% compute codifies this difficulty-based heuristic into learned routers
% and confidence-driven early-stopping policies. The implicit
% assumption underlying all of these methods is that compute buys
% accuracy, and that accuracy is fungible across tasks.

% This assumption breaks in real deployment. Behind a given accuracy,
% the ``remaining error'' carries wildly different costs across tasks:
% a typo in a log message and a database migration that silently
% corrupts a production table both count as a single unit of accuracy
% on a benchmark, but only one of them is worth additional compute to
% avoid. We argue that test-time compute should be allocated by
% expected cost-of-error, not by predicted difficulty.

% We propose \emph{consequence-aware} test-time compute allocation. At
% its core is a lightweight predictor that estimates the deployment-time
% cost-of-error of each task directly from its issue text; a scheduler
% then routes higher-consequence tasks to larger compute tiers---or
% larger thinking budgets---under the same total budget as a
% difficulty-based baseline.

% We evaluate on 700 software-engineering tasks (SWE-bench Lite and
% Multi-SWE-bench mini), labeled under three independent pipelines, and
% audit three contemporary thinking models. Across all labeling
% pipelines, consequence is statistically orthogonal to difficulty and
% remains a significant predictor of cost-weighted gain after
% controlling for difficulty and cross-model confidence. Current
% thinking models do not allocate compute by consequence: their spending
% is either uncorrelated with consequence, saturated at the token cap,
% or weakly leaning but far below the required magnitude. Our
% issue-only predictor never misclassifies a high-consequence task as
% low-consequence in 300 SWE-bench tasks. Under matched compute budgets,
% consequence-aware routing reduces cost-weighted loss by more than
% $30\%$ relative to difficulty-aware routing, and the predictor-driven
% variant retains over $90\%$ of the oracle gain. Notably,
% difficulty-aware routing performs worse than random because the
% hardest tasks are unsolvable at any tier.
Modern reasoning models already enable the allocation of different amounts of compute at test time for different tasks. In response, existing adaptive test-time computation methods typically follow a difficulty heuristic, allocating more thinking tokens, model calls, or compute budgets to tasks predicted to be harder. However, this strategy implicitly assumes that errors across tasks can be treated homogeneously under a single accuracy objective—an assumption we argue does not hold for real-world deployment. After all, a typo in a log and a migration that corrupts a production database each count as a single failure in a benchmark, yet their real-world costs are completely different.
To address this gap, this paper proposes consequence-aware test-time compute allocation, a new paradigm that allocates test-time compute according to the consequences of errors. Specifically, we use a lightweight predictor that estimates, solely from the issue text, the deployment cost a task would incur if it fails. Based on this prediction, the system routes high-consequence tasks to larger compute tiers or higher thinking budgets under the same total budget. We conduct main experiments on SWE-bench Lite and validate on Multi-SWE-bench mini, covering a total of 700 software engineering tasks.
Our results reveal that consequence and difficulty are approximately orthogonal under various annotations, and that current thinking models do not allocate compute sufficiently according to consequence. Moreover, our issue-only predictor never misclassifies a high-consequence task as low-consequence across the 300 SWE-bench tasks. Under matched compute budgets, consequence-aware routing reduces cost-weighted loss by more than 30\% relative to difficulty-aware routing, while the predictor-driven variant retains over 90\% of the oracle gain. Notably, difficulty-aware routing performs worse than random, as the hardest tasks are unsolvable at any tier. Taken together, these findings suggest that test-time compute should focus not only on how hard a task is, but also on how expensive an error would be and whether extra compute is worth it.


\end{abstract}

\section{Introduction}
\label{sec:intro}

Modern reasoning models---OpenAI's o-series, DeepSeek-R1, Anthropic's
Claude with extended thinking, and Qwen3-Thinking---vary how many
tokens they spend per task. Whether through learned
routers~\citep{damani2024learning}, compute-difficulty scaling
laws~\citep{snell2024scaling}, or confidence-driven early
stopping~\citep{manvi2024adaptive}, recent work on adaptive test-time
compute converges on a single heuristic: \emph{spend more on harder
tasks}.

The implicit assumption underlying this heuristic is that extra
compute is purchased to raise expected accuracy, and that accuracy
is fungible across tasks. This assumption holds on benchmarks---where
each task contributes a single fail/pass increment to a global
score---but breaks in real deployment. Consider two failures a
coding assistant might produce on the same day: a typo in a log
message, and a database migration that silently corrupts a production
table under a race condition. On a benchmark, both count as one
failed task. In deployment, only the second is worth additional
compute to avoid. The cost of the remaining error is wildly
heterogeneous across tasks, and a scheduler that treats them all the
same is leaving most of its risk-reduction budget on the floor.

We argue that test-time compute should be allocated by expected
cost-of-error, not by predicted difficulty. Formally, the deployment
objective is not the maximize-accuracy-subject-to-budget formulation
\[
\max_{\pi}\ \mathrm{acc}(\pi) - \lambda\,c(\pi),
\]
adopted by current adaptive-compute work, but a cost-weighted risk
minimization
\begin{equation}\label{eq:objective}
c^{\ast}(x) \;=\; \arg\min_{c}\ \alpha\!\cdot\!c \;+\; \mathrm{cost}(x)\cdot P(\mathrm{err}\mid x, c),
\end{equation}
where $\mathrm{cost}(x)$ is the realized loss if the model gets task
$x$ wrong. The closest prior framing---rational
metareasoning~\citep{russell1991right, hay2012selecting,
yang2024rational}---already includes a utility term in its
decision-theoretic foundation, but modern LLM operationalizations
implicitly take $\mathrm{cost}(x)\equiv 1$, collapsing the objective
back to a difficulty-only regime.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_motivation.pdf}
\caption{\textbf{Three contemporary thinking models fail to allocate
compute by consequence.} Each panel shows one model's relationship
between actual thinking length and our consequence label on
SWE-bench Lite tasks. Qwen3-8B's thinking length is uncorrelated with
consequence ($\rho = 0.002$). Qwen3-VL-8B-Thinking pins $99.3\%$ of
tasks at its 8192-token cap. Claude Sonnet 4.5 with extended thinking
shows a statistically significant but quantitatively inadequate
leaning ($\rho = 0.20$, $p = 0.008$, $n = 171$). Across three models
with three different scheduling mechanisms, none allocates compute by
consequence at the magnitude rational decision-making requires.}
\label{fig:motivation}
\end{figure}

An intuitive defense of the difficulty-default would be that strong
reasoning models, even without an explicit cost signal, might allocate
compute by consequence emergently. Figure~\ref{fig:motivation} reports
the audit. Across three contemporary thinking models with three
different scheduling mechanisms, none allocates compute by consequence
at the magnitude rational decision-making requires: Qwen3-8B's
thinking length is essentially uncorrelated with consequence
($\rho = 0.002$), Qwen3-VL-8B-Thinking pins $99.3\%$ of tasks at its
8192-token cap, and Claude Sonnet 4.5 shows a statistically
significant but quantitatively inadequate leaning ($\rho = 0.20$,
$p = 0.008$, $n = 171$; high-consequence tasks receive only $20.5\%$
more thinking than low-consequence ones---an order of magnitude below
what Equation~\eqref{eq:objective} would prescribe).

We propose \emph{consequence-aware test-time compute allocation}. A
lightweight predictor estimates the deployment-time cost-of-error of
each task directly from its issue text. A scheduler then routes
higher-consequence tasks to larger compute tiers---or, equivalently,
larger thinking budgets---under the same total compute budget as a
difficulty-based baseline. No reasoning model is retrained; the
intervention sits entirely at the scheduling layer.

The remainder of the paper builds the empirical case in five steps.
\begin{itemize}
\item[(1)] \textbf{Consequence is statistically independent of
difficulty, and is not a re-skinned confidence signal.} On 700
software-engineering tasks (300 SWE-bench Lite and 400
Multi-SWE-bench mini) labeled under three independent
pipelines---rule-based, LLM-with-patch, and LLM-issue-only---Spearman
$\rho(\mathrm{consequence}, \mathrm{difficulty}) \approx 0$ holds
under every labeling. Partialing out difficulty \emph{and}
cross-model pass-rate variance (a confidence proxy), consequence
still explains an additional $26.4$ percentage points of cost-weighted
marginal value ($+55.7\%$ relative; \S\ref{sec:cons}).

\item[(2)] \textbf{Existing thinking models do not allocate by
consequence}, in three distinct failure modes
(\S\ref{sec:audit}, Figure~\ref{fig:motivation}): no signal
(Qwen3-8B), saturation (Qwen3-VL-Thinking), and emergent-but-inadequate
leaning (Claude Sonnet 4.5).

\item[(3)] \textbf{Consequence can be predicted from issue text alone,
and predicted safely for deployment.} A Claude-issue-only predictor
agrees with the with-patch reference at Cohen's $\kappa = 0.572$
(within-model) and $\kappa = 0.379$ (cross-model). Crucially, in
$300$ SWE-bench tasks the predictor \emph{never} misclassifies a
high-consequence task as low-consequence ($\mathrm{class}\,2 \to
\mathrm{class}\,0$ error rate $= 0\%$): even when wrong, it errs to
the adjacent class, never across the safety gap
(\S\ref{sec:pred}).

\item[(4)] \textbf{Consequence-aware allocation reduces cost-weighted
loss by over $30\%$ relative to difficulty-aware under matched total
compute,} on a $16$-model SWE-bench compute-tier benchmark; the
predictor-driven variant retains over $90\%$ of the oracle gain
(\S\ref{sec:method}, \S\ref{sec:exp}).

\item[(5)] \textbf{Difficulty-aware allocation is in fact worse than
random,} and we surface the root cause:
$\rho(\mathrm{difficulty}, \Delta\mathrm{success}) = -0.78$, meaning
the hardest tasks have the \emph{lowest} marginal gain from extra
compute---they are unsolvable at any tier. A top-$25\%$ selection
ranked by difficulty captures \emph{zero percent} of the
cost-weighted gain available in the data, versus $43\%$ for
consequence-ranked and $24\%$ for random
(\S\ref{sec:exp}).
\end{itemize}

\section{Related Work}

\textbf{Adaptive test-time compute.} A growing body of work studies how
much reasoning a language model should perform per input.
\citet{snell2024scaling} establish compute--difficulty scaling laws,
showing that the optimal token budget on a given task depends on its
predicted difficulty. \citet{damani2024learning} train a reward
predictor that allocates compute according to expected accuracy gain.
\citet{manvi2024adaptive} use token-level confidence to stop generation
early on tasks where the model is already confident. All three
methods operationalize task value through a difficulty or
confidence proxy, and none introduces a per-task cost-of-error term.
Our work complements this line: we keep the reasoning model unchanged
but route compute according to a different signal.

\textbf{Rational metareasoning.} The decision-theoretic framing of
computation as a costly action that should only be performed when its
expected utility outweighs its cost goes back to
\citet{russell1991right} and \citet{hay2012selecting}. In their
formulation, the value of computation explicitly includes the
\emph{utility} of correcting an error---exactly the term that modern
LLM operationalizations drop. \citet{yang2024rational} apply this
framework to LLMs but assume uniform utility per task, so their
realized objective reduces to accuracy-vs-cost rather than risk-vs-cost.
Equation~\eqref{eq:objective} reintroduces the per-task utility term
their formulation drops.

\textbf{Cost-sensitive learning and selective prediction.}
\citet{elkan2001foundations} formalizes cost-sensitive learning for
classification with asymmetric error costs, and selective-prediction
methods such as \citet{geifman2017selective} let a model abstain on
inputs where it is uncertain. Both lines acknowledge that errors are
not interchangeable, but they apply this idea at \emph{prediction
time}---deciding whether to answer. We apply the same principle at
\emph{compute time}---deciding how much to think.

\textbf{Software-engineering benchmarks.} We use SWE-bench
Lite~\citep{jimenez2023swebench} as our primary testbed and a
400-task subset of Multi-SWE-bench~\citep{ma2024multiswebench} for
cross-domain validation. Real bug reports span a wide consequence
range (cosmetic typo to silent data loss) at a wide difficulty
range, and the curated ground-truth patches enable LLM-judge
evaluation.

\section{Consequence as a Construct}
\label{sec:cons}

We first define consequence as a measurable property of a task,
describe three independent labeling pipelines, and show that
consequence is statistically distinct from both difficulty and
model-disagreement-based confidence.

\textbf{Definition.} The \emph{consequence} of a task is an ordinal
label in $\{0, 1, 2\}$ that captures the realized deployment cost
should the fix be wrong:
\begin{itemize}\itemsep0pt
  \item \textbf{0 (low).} Cosmetic, format, documentation, or
        log-message errors. The user notices, but nothing downstream is
        corrupted.
  \item \textbf{1 (medium).} Functional but contained bugs: an API
        returns the wrong value, an edge case crashes, a feature is
        unavailable. The error is observable and locally recoverable.
  \item \textbf{2 (high).} Silent data corruption, security or
        permission bypass, irreversible operations (delete / overwrite
        / migration), or a wrong result that is consumed downstream
        without being visible.
\end{itemize}

\textbf{Labeling pipelines.} We instantiate three independent labelers
on each task in SWE-bench Lite ($N{=}300$) and Multi-SWE-bench mini
($N{=}400$).
(i) A \emph{rule-based} labeler triggers on lexical patterns in the
modified file path and the gold patch (e.g., presence of
\texttt{delete}, \texttt{auth}, \texttt{migration}).
(ii) An \emph{LLM-with-patch judge} (Qwen3-8B) sees the issue text,
gold patch, and modified files, and produces $\{0,1,2\}$. We treat
this as the \textbf{primary} consequence label throughout the paper,
because it has access to the realized fix.
(iii) An \emph{LLM-issue-only predictor} (\S\ref{sec:pred}) sees only
the issue text and file path---matching the information a
deployment-time scheduler has---and is used to test whether
consequence can be inferred \emph{before} solving.

\textbf{Consequence is orthogonal to difficulty.} For each task we
compute difficulty as $1 - \overline{\mathrm{passrate}}$ across $16$
public SWE-bench leaderboard solvers. Under both the rule-based and
LLM-with-patch labelings, the rank correlation between consequence and
difficulty is statistically indistinguishable from zero:
$\rho_{\mathrm{rule}}{=}{+}0.047$ ($p{>}0.4$) and
$\rho_{\mathrm{LLM\text{-}patch}}{=}{-}0.096$ ($p{>}0.09$). The result
also replicates under a $150$-task human annotation study with three
independent labelers ($\rho_{\mathrm{human}}{=}{-}0.066$,
$p > 0.5$; see Appendix~\ref{app:iaa}) and under the issue-only
predictor of \S\ref{sec:pred} ($\rho = -0.109$, $p = 0.06$). All
four labelings---rule, LLM-with-patch, LLM-issue-only, human
majority---place the correlation within $\pm 0.11$ of zero, none
significant (Appendix~\ref{app:rubric}). The orthogonality is not an
artefact of any single labeling pipeline. High-stakes tasks are
spread uniformly across the difficulty range: trivial typos can have
high consequence (security-critical log redaction), and extremely hard
tasks can have low consequence (an obscure parser edge case). This is
the central premise of the paper: difficulty does not predict
consequence, so a difficulty-only scheduler is, by construction, blind
to deployment risk.

A natural follow-up question---whether consequence is merely a
re-skinning of cross-model disagreement (i.e., confidence) used by
existing adaptive-compute schedulers---is deferred to
\S\ref{sec:whyfail}, where we use it to explain \emph{why}
difficulty-aware allocation fails.

\section{Model Audit: Do Existing Thinkers Allocate by Consequence?}
\label{sec:audit}

If a strong reasoning model, trained on enough data, already
allocates compute by consequence emergently, our paper's premise
weakens substantially. We therefore audit three contemporary thinking
models with different compute-allocation mechanisms, measuring whether
their actual thinking length scales with the LLM-with-patch
consequence label (Figure~\ref{fig:motivation}). The three models
exemplify three distinct failure modes.

\textbf{Qwen3-8B (hybrid thinking).} Qwen3-8B~\citep{qwen2025qwen3}
alternates between a ``thinking'' and a ``direct'' mode by emitting a
control token. On
$50$ SWE-bench Lite tasks at \texttt{max\_new\_tokens}$=4096$, the
thinking-character count varies widely across tasks, but the rank
correlation with consequence is
$\rho(\mathrm{thinking}, \mathrm{cons}) = +0.002$. The model decides
how long to think---and what it decides has essentially no
relationship to how costly an error would be.

\textbf{Qwen3-VL-8B-Thinking (dedicated thinking).} The dedicated
thinking variant produces a separate reasoning stream subject to a
configurable budget. On the same 50 tasks at
\texttt{max\_new\_tokens}$=8192$, the model pins $99.3\%$ of tasks at
the token cap, leaving no headroom for consequence-driven
differentiation. The failure mode here is not random allocation but
\emph{saturation}: the model wants to think for longer than the budget
allows on essentially every task, regardless of its cost.

\textbf{Claude Sonnet 4.5 with extended thinking.} We run
Claude~\citep{anthropic2025claude45} with an extended-thinking budget
of $16{,}000$ tokens on $171$ stratified
SWE-bench Lite tasks. Here we observe a statistically significant
relationship between thinking length and consequence:
$\rho = +0.203$, $p = 0.008$. The magnitude, however, is far below
what Equation~\eqref{eq:objective} would prescribe: averaged across
the sample, high-consequence (class~2) tasks receive only $20.5\%$
more thinking than low-consequence (class~0) tasks ($6135$ versus
$5090$ characters on average), while the median high-consequence task
has $\approx 5\times$ the marginal value of extra compute
(\S\ref{sec:exp}). The frontier model exhibits a weak emergent leaning
toward consequence-aware allocation, but it is \emph{quantitatively
inadequate} for deployment.

Across the three models we audit---one with random spending, one
saturated, and one weakly-but-inadequately leaning---none allocates
compute by consequence at the magnitude rational decision-making
requires (Table~\ref{tab:audit}). The question that motivates the rest
of the paper is therefore not ``does the model already do this?'' but
``how should a deployer wrap a reasoning model so that it does?''

\begin{table}[h]
\centering
\caption{\textbf{Model audit on SWE-bench Lite.} None of the three
audited reasoning models scales thinking length with consequence at
the magnitude that Eq.~\ref{eq:objective} prescribes. Reported
quantities: sample size $n$, configured token budget $\mathrm{max\_new}$,
Spearman rank correlation between thinking length and consequence,
and the resulting failure mode.}
\label{tab:audit}
\small
\begin{tabular}{lrrcccl}
\toprule
Model & $n$ & $\mathrm{max\_new}$ & $\rho(\text{thinking}, \text{cons})$ & $p$ & High vs Low & Failure mode \\
\midrule
Qwen3-8B (hybrid)         & $50$  & $4{,}096$  & $+0.002$ & n.s.     & ---     & no signal \\
Qwen3-VL-8B-Thinking      & $50$  & $8{,}192$  & ---      & ---      & ---     & $99.3\%$ saturated \\
Claude Sonnet~4.5 (ext.~thinking) & $171$ & $16{,}000$ & $+0.203$ & $0.008$ & $+20.5\%$ & emergent but inadequate \\
\bottomrule
\end{tabular}
\end{table}

\section{Predicting Consequence at Deployment Time}
\label{sec:pred}

A deployment-time scheduler cannot inspect a gold patch. It can only
read the issue text and the path of the affected file. We therefore
ask: can consequence be predicted before the fix is computed, and
with enough safety to deploy?

\textbf{Setup.} We strip the gold patch and modified-file content
from the input to the LLM judge of \S\ref{sec:cons}, leaving only the
GitHub issue text and the path of the file the issue refers to. The
\textbf{primary predictor} is Qwen3-8B in issue-only mode, evaluated
against the with-patch reference of the same model. As a
cross-model robustness check we additionally evaluate Claude Sonnet
4.5 in issue-only mode against the same Qwen with-patch reference.
Both predictors return $\{0, 1, 2\}$ in a single forward pass.

\textbf{Agreement with the with-patch label.} The primary predictor
reaches Cohen's $\kappa = 0.572$ on all $300$ SWE-bench Lite tasks,
which is substantial agreement on a three-class ordinal task. The
cross-model Claude predictor reaches $\kappa = 0.379$
(fair-to-moderate)---lower, as expected for a different model family,
but still well above chance. Both predictors are high enough to use
as routing signals in \S\ref{sec:exp}.

\textbf{The errors that matter.} Aggregate $\kappa$ does not capture
the asymmetry that matters for deployment: under-allocating compute on
a high-consequence task is much worse than over-allocating on a
low-consequence one. We therefore decompose the confusion matrix of
the primary predictor on the 300 SWE-bench Lite tasks. Of the $44$
tasks the with-patch judge labels class~2, the primary predictor
labels $39$ as class~2 (class-2 recall $= 88.6\%$) and the remaining
$5$ as class~1; \emph{not a single class-2 task is predicted as
class~0} (severe under-allocation rate $= 0/44 = 0\%$). The
cross-model Claude predictor matches this safety property
(class-2$\,\to\,$class-0 $= 0/44$) while having lower aggregate
recall, suggesting that the zero-severe-error property is rubric- and
text-driven rather than model-specific (Table~\ref{tab:predictor};
Figure~\ref{fig:confusion}).
Full per-class metrics for both predictors are reported in
Appendix~\ref{app:predictor}.

\begin{table}[h]
\centering
\caption{\textbf{Predictor calibration vs.\ the LLM-with-patch
reference} on 300 SWE-bench Lite tasks. The deployment-critical
``never miss a high-consequence task as low'' property
(Class-2$\,\to\,$Class-0 $= 0/44$) holds for both predictors,
even though the cross-model variant has substantially lower
aggregate $\kappa$ and class-2 recall---suggesting the safety property
is text- and rubric-driven, not model-specific.}
\label{tab:predictor}
\small
\begin{tabular}{lcccccc}
\toprule
Predictor & $\kappa$ & Class-2 recall & 2$\to$0 & 2$\to$1 & Severe (0$\leftrightarrow$2) \\
\midrule
Qwen3-8B issue-only \emph{(within-model, primary)}
   & $\mathbf{0.572}$ & $\mathbf{88.6\%}$ \,($39/44$) & $\mathbf{0/44}$ & $5/44$  & $\mathbf{0.0\%}$ \,($0/300$) \\
Claude Sonnet~4.5 issue-only \emph{(cross-model)}
   & $0.379$          & $52.3\%$ \,($23/44$)          & $\mathbf{0/44}$ & $21/44$ & $0.7\%$ \,($2/300$) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{fig_confusion.pdf}
\caption{\textbf{Predictor confusion matrices} against the
LLM-with-patch reference on 300 SWE-bench Lite tasks. Rows: true
class; columns: predicted class. The deployment-critical
under-allocation cell (\emph{true class 2, predicted class 0}) is
empty for both predictors, highlighted by the dashed red rectangle.
The within-model Qwen predictor (left) has substantially higher
class-2 recall ($39/44$) than the cross-model Claude predictor
(right; $23/44$), but the safety property holds in both cases.}
\label{fig:confusion}
\end{figure}

This pattern---imperfect but safety-respecting---is what makes the
predictor deployable. Even when the predictor is wrong, it almost
always errs to the adjacent class, so a downstream scheduler will at
worst route a high-consequence task to a medium budget rather than to
the smallest budget. Section~\ref{sec:exp} confirms this empirically:
the predictor-driven routing retains over $90\%$ of the gain achieved
by oracle-consequence routing.

\section{Method: Cost-Weighted Compute Allocation}
\label{sec:method}

We now specify how a scheduler should use the consequence signal.
The setup is a multi-tier compute system: a finite set of compute
\emph{tiers} $\{T_1, \ldots, T_K\}$ ordered from cheap to expensive,
where each tier is either a different model family, a different
sampling temperature, a different self-consistency budget, or a
different thinking-token budget on the same model. The deployer has a
total compute budget $B$ to spend across a stream of tasks $X$.

\textbf{Objective.} Minimizing the cost-weighted loss on the task
stream under a total-compute constraint is
\begin{equation}\label{eq:budgetobj}
\min_{T:\,X\to\{T_1,\dots,T_K\}}\ \sum_{x\in X}\, \mathrm{cost}(x)\cdot
\bigl[\,1 - p\bigl(\text{correct}\mid x, T(x)\bigr)\bigr]
\quad\text{s.t.}\quad
\sum_{x\in X} c(T(x)) \leq B,
\end{equation}
where $c(T_k)$ is the per-task compute price of tier $T_k$. With
$\mathrm{cost}(x){\equiv}1$, Eq.~\eqref{eq:budgetobj} reduces to plain
accuracy maximization under a compute budget---the objective implicit
in prior adaptive-compute work.

\textbf{Algorithm.} We instantiate Eq.~\eqref{eq:budgetobj} with a
simple decoupled scheme. Given the predicted consequence
$\hat{c}(x) \in \{0,1,2\}$ from \S\ref{sec:pred} and a target overall
compute budget $B$, we sort tasks by $\hat{c}(x)$ in descending order
and assign them to compute tiers from premium to cheap until the
budget is exhausted (Algorithm~\ref{alg:cons-route}). In the
fixed-quota variants of \S\ref{sec:exp}, the top-$q\%$ tasks by
$\hat{c}$ go to the premium tier and the rest go to the cheap tier;
$q$ is chosen so that total compute matches the difficulty-based
baseline. This decoupled scheme assumes a monotone relationship
between $\hat{c}$ and the marginal value of more compute---an
assumption we test directly in \S\ref{sec:exp}.

\begin{algorithm}
\caption{Cost-weighted compute allocation}\label{alg:cons-route}
\begin{algorithmic}[1]
\REQUIRE Tasks $X$; predictor $\hat{c}$; tiers $T_1, \ldots, T_K$ with prices $c(T_k)$; total budget $B$.
\STATE Score each task: $s(x) \leftarrow \hat{c}(x)$
\STATE Sort $X$ by $s(\cdot)$ in descending order
\STATE Initialize spent $\leftarrow 0$, $T(x) \leftarrow T_1$ for all $x$
\FOR{$k = K$ down to $2$}
  \FOR{$x$ in sorted order}
    \IF{$T(x) = T_1$ and spent $+ [c(T_k) - c(T_1)] \leq B - \sum_{x'} c(T(x'))$}
      \STATE $T(x) \leftarrow T_k$
      \STATE spent $\leftarrow$ spent $+ [c(T_k) - c(T_1)]$
    \ENDIF
  \ENDFOR
\ENDFOR
\RETURN $T$
\end{algorithmic}
\end{algorithm}

The scheme has three properties worth highlighting. First, it does
not retrain any reasoning model; it sits entirely at the scheduling
layer. Second, the predictor only needs to be \emph{ordinal-correct}
on the safety dimension---ranking class-2 tasks above class-0 tasks
matters far more than getting the absolute label right. Third, the
scheme degrades gracefully: with a perfect predictor we recover
oracle-consequence routing, with a random predictor we recover random
routing, and with the difficulty score substituted for $\hat{c}$ we
recover the Snell-style difficulty-aware baseline.

\section{Consequence-Aware Allocation Reduces Cost-Weighted Loss}
\label{sec:exp}

We test Algorithm~\ref{alg:cons-route} on a 16-model SWE-bench
compute-tier benchmark.

\textbf{Setup.} We aggregate the per-task resolution status of 16
public SWE-bench leaderboard solvers spanning four years of model
generations (RAG baselines on Claude 2 and GPT-4, SWE-agent on
Claude 3 Opus and Claude 3.5 Sonnet, more recent agentic systems on
Claude 4 Sonnet including SWE-agent, ExpeRepair, and KGCompass). We
sort the 16 models by total resolved count and form two tiers: a
\emph{cheap} tier of the bottom 4 models and a \emph{premium} tier of
the top 4 models. For each task, $p_{\mathrm{cheap}}(x)$ is the
empirical fraction of cheap-tier solvers that resolve the task, and
$p_{\mathrm{premium}}(x)$ analogously for the premium tier. We assume
a fixed compute price ratio $c(\text{premium})/c(\text{cheap}) = 4$,
consistent with the model-tier price gap on commercial APIs.

\textbf{Strategies.} We compare seven allocation strategies under a
matched total compute budget. \emph{Random} routes a uniformly random
top-$q\%$ subset of tasks to the premium tier. \emph{Difficulty-aware
(Snell-style)} routes the top-$q\%$ by predicted
difficulty~\citep{snell2024scaling}. \emph{Consequence-aware
(predictor)} routes the top-$q\%$ by the issue-only consequence
predictor of \S\ref{sec:pred}, in two variants: the within-model Qwen
predictor (\textbf{primary deployable variant}) and the cross-model
Claude predictor (robustness check).
\emph{Consequence-aware (oracle)} substitutes the LLM-with-patch
label for the predictor and isolates the upper bound of the
one-signal predictor. \emph{Priority-aware (predictor / oracle)}
routes the top-$q\%$ by $\hat{c}(x) \cdot [p_{\mathrm{premium}}(x) -
p_{\mathrm{cheap}}(x)]$, a two-signal variant that further accounts
for the marginal value of extra compute on each task.

\textbf{Metric.} Cost-weighted loss
$\mathcal{L}(T) = \sum_x \mathrm{cons}(x)\cdot[\,1 - p_{T(x)}(x)\,]$
on $N = 300$ tasks at the matched-compute operating point of routing
the top-$25\%$ of tasks to the premium tier.

\begin{table}[t]
\centering
\caption{\textbf{Main results.} Cost-weighted loss $\mathcal{L}$,
relative reduction over the difficulty-aware baseline, and average
accuracy on $N{=}300$ SWE-bench Lite tasks at matched total compute
(top-$25\%$ premium routing; cheap/premium price ratio $1{:}4$).
Difficulty-aware allocation is the worst strategy, worse even than
Random. The primary deployable variant
(Priority-aware with the Qwen issue-only predictor) recovers
$92.9\%$ of the priority-oracle gain. Even the simplest
consequence-only variant retains $93.6\%$ of its corresponding oracle.}
\label{tab:main}
\small
\begin{tabular}{lrrr}
\toprule
Strategy & $\mathcal{L}$ & $\Delta$ vs Diff & Accuracy \\
\midrule
Difficulty-aware (Snell-style)~\citep{snell2024scaling}              & $268.25$ & ~~$0\%$ baseline & $0.054$ \\
Random                                                                & $233.00$ & $+13.1\%$ & $0.170$ \\
\midrule
Consequence-aware (Claude predictor; cross-model robustness)          & $220.50$ & $+17.8\%$ & $0.174$ \\
Consequence-aware (Qwen predictor; deployable)                        & $209.75$ & $+21.8\%$ & $0.184$ \\
Consequence-aware (oracle)                                            & $205.75$ & $+23.3\%$ & $0.187$ \\
\midrule
Priority-aware (Qwen predictor; \textbf{primary deployable variant})  & $\mathbf{186.00}$ & $\mathbf{+30.7\%}$ & $\mathbf{0.274}$ \\
Priority-aware (oracle; upper bound)                                  & $179.75$ & $+33.0\%$ & $0.278$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_pareto.pdf}
\caption{\textbf{Pareto curve: cost-weighted loss vs total compute}
on the 16-model SWE-bench compute-tier benchmark. Each curve sweeps
the fraction of tasks routed to the premium tier from $0$ to $100\%$.
The Difficulty-aware curve (Snell-style) is flat across most of the
range and is dominated by the Random baseline---additional premium
compute spent on the hardest tasks does not reduce cost-weighted
loss, because those tasks are unsolvable at any tier (see
\S\ref{sec:marginal}). The Priority-aware variants Pareto-dominate
all others, and the Qwen-predictor-driven version
(\textbf{primary deployable variant}) tracks the oracle closely
across the entire budget axis. The dotted vertical line marks the
operating point used in Table~\ref{tab:main}.}
\label{fig:pareto}
\end{figure}

\textbf{Consequence-aware allocation beats difficulty-aware by
$30\%{+}$.} Table~\ref{tab:main} reports the main results;
Figure~\ref{fig:pareto} shows the corresponding Pareto curve across
the full budget axis. The
primary deployable variant---Priority-aware routing driven by the
issue-only Qwen predictor---reduces cost-weighted loss by $30.7\%$
relative to the difficulty-aware Snell-style baseline. Its oracle
counterpart (using the LLM-with-patch labels) reduces it by $33.0\%$;
the predictor retains $92.9\%$ of the oracle gain. A simpler
consequence-only variant (without the $\Delta\mathrm{success}$ term)
already produces a $21.8\%$ reduction, retaining $93.6\%$ of its
corresponding oracle, confirming that the safety-respecting predictor
of \S\ref{sec:pred} translates almost losslessly into routing quality.

\textbf{Difficulty-aware is in fact worse than random.} The most
striking row of Table~\ref{tab:main} is that the difficulty-aware
strategy underperforms even the random baseline by $13$ percentage
points. A top-$25\%$ selection ranked by difficulty captures $0\%$
of the cost-weighted gain available in the data, versus $24\%$ for
random, $43\%$ for the consequence-only oracle, and $59\%$ for the
priority oracle. The difficulty strategy is not merely suboptimal:
under the cost-weighted objective it is anti-correlated with the
right answer. The next section explains why.

\section{Why Difficulty-Aware Allocation Fails}
\label{sec:whyfail}

The results of \S\ref{sec:exp} demand an explanation. Why does a
strategy that allocates more compute to harder tasks do worse than
allocating randomly? We answer in two parts. First, we rule out the
alternative interpretation that consequence is merely a re-skinning
of cross-model disagreement (the signal that current confidence-based
adaptive-compute methods already use). Then we examine per-task
marginal utility and show that difficulty is anti-correlated with
where compute actually helps.

\subsection{Confounding ablation: consequence is not a re-skin of confidence}
\label{sec:p0b}

A reviewer might argue that consequence works only because it
inadvertently picks up another signal that current adaptive-compute
methods already exploit: cross-model disagreement on a task, which
acts as a confidence proxy. To rule this out, we construct
\[
Y(x) \;=\; \mathrm{cons}(x)\cdot \bigl[\,p_{\mathrm{premium}}(x) - p_{\mathrm{cheap}}(x)\,\bigr],
\]
the cost-weighted marginal value of premium compute on task $x$, and
test whether consequence carries information about $Y$ beyond what
difficulty and per-task pass-rate variance (the confidence proxy)
already explain. The partial Spearman correlation, controlling for
both,
\[
\rho\bigl(\,\mathrm{cons},\, Y \,\big|\, \mathrm{diff},\, \mathrm{passvar}\,\bigr) = +0.696,\quad p < 10^{-43}.
\]
A hierarchical regression on $Y$ confirms the gap
(Table~\ref{tab:r2decomp}):
$R^2_{\mathrm{diff}+\mathrm{passvar}} = 0.474$, whereas
$R^2_{\mathrm{diff}+\mathrm{passvar}+\mathrm{cons}} = 0.738$. Adding
consequence on top of difficulty and confidence yields
$\Delta R^2 = 26.4$ percentage points ($+55.7\%$ relative). Notably,
difficulty alone contributes essentially no incremental variance once
confidence is in the model
($R^2_{\mathrm{diff}+\mathrm{passvar}} - R^2_{\mathrm{passvar}} = 0.0004$),
so the $30\%{+}$ deployment gain in Table~\ref{tab:main} is not
produced by recovering a difficulty or confidence signal in disguise.

\begin{table}[h]
\centering
\caption{\textbf{R\textsuperscript{2} decomposition on the
cost-weighted marginal value $Y$.} Difficulty alone explains $28\%$ of
variance and pass-rate variance (the standard confidence proxy)
explains $47\%$. Adding consequence on top of both lifts $R^2$ from
$0.474$ to $0.738$, an absolute gain of $26.4$ percentage points
($+55.7\%$ relative). Difficulty contributes essentially zero variance
once confidence is already in the model.}
\label{tab:r2decomp}
\small
\begin{tabular}{lc}
\toprule
Predictors in linear regression on $Y = \mathrm{cons}\cdot\Delta\mathrm{success}$ & $R^{2}$ \\
\midrule
Difficulty only                                       & $0.281$ \\
Pass-variance only (confidence proxy)                 & $0.474$ \\
Consequence only                                      & $0.350$ \\
Difficulty + Pass-variance                            & $0.474$ \\
Difficulty + Pass-variance + \textbf{Consequence}     & $\mathbf{0.738}$ \\
\midrule
$\Delta R^{2}$ from adding Consequence                & $\mathbf{+0.264}$ \;($+55.7\%$ relative) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Marginal utility: hardest tasks are unsolvable at any tier}
\label{sec:marginal}

Let $\Delta\mathrm{success}(x) = p_{\mathrm{premium}}(x) -
p_{\mathrm{cheap}}(x)$ be the marginal accuracy gain from upgrading
$x$ from cheap to premium. Three observations explain the failure of
difficulty-aware allocation.

\textbf{(1) Difficulty is anti-correlated with marginal gain.}
\(\rho(\mathrm{difficulty}, \Delta\mathrm{success}) = -0.78\)
($p < 10^{-60}$). The hardest tasks---those on which all 16 models
struggle---are also the tasks where even the premium tier does not
help. A difficulty-aware scheduler therefore concentrates its
upgrade budget on tasks that cannot be saved at the available
premium tier.

\textbf{(2) Consequence is uncorrelated with marginal gain.}
\(\rho(\mathrm{cons}, \Delta\mathrm{success}) = +0.09\) (n.s.).
Consequence does not predict whether extra compute helps on a given
task. What it does predict is whether the help, if it happens, is
\emph{valuable}.

\textbf{(3) Consequence and marginal gain combine multiplicatively.}
The priority signal
\(\mathrm{priority}(x) = \mathrm{cons}(x)\cdot\Delta\mathrm{success}(x)\)
ranks tasks by their cost-weighted return on extra compute. The
ranking in Table~\ref{tab:main} (priority $>$ consequence-only $>$
random $>$ difficulty) matches this decomposition exactly: a
top-$25\%$ selection by priority captures $59\%$ of the data's total
cost-weighted gain, against $43\%$ for consequence, $24\%$ for
random, and $0\%$ for difficulty. The fact that consequence alone
captures $43\%$ versus priority's $59\%$ also bounds how much can be
gained by moving from a one-signal predictor to a two-signal one:
meaningful, but not transformative---consistent with the
$+21.8\%$-vs-$+30.7\%$ gap in Table~\ref{tab:main}.

Together, (1)--(3) explain the negative result on difficulty-aware
allocation: a cost-blind scheduler that spends more compute on harder
tasks is spending its budget where extra compute has the lowest
marginal return. Figure~\ref{fig:marginal} visualizes the effect---
$\Delta\mathrm{success}$ rises with difficulty up to the moderate
range and then collapses on the unsolvable tail, producing the
strongly negative Spearman correlation. Appendix~\ref{app:marginal}
reports the per-task view: the top-ranked tasks by priority all have
moderate difficulty and large $\Delta\mathrm{success}$, while the
top-ranked tasks by difficulty all have $\Delta\mathrm{success} = 0$.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_marginal.pdf}
\caption{\textbf{Why difficulty-aware allocation fails: $\Delta$success
collapses on the hardest tasks.} Each marker is one of the 300
SWE-bench Lite tasks, colored by consequence class.
$\Delta\mathrm{success} = p_{\mathrm{premium}}(x) - p_{\mathrm{cheap}}(x)$
is the marginal accuracy gain from routing $x$ to the premium tier.
The black trend line (binned mean) rises with difficulty up to the
moderate range and then collapses on the unsolvable tail, producing
the strongly negative Spearman correlation
($\rho = -0.78$, $p < 10^{-60}$). A scheduler that allocates compute
to the difficulty tail spends its budget where extra compute returns
nothing.}
\label{fig:marginal}
\end{figure}

\section{Discussion and Limitations}
\label{sec:discussion}

\textbf{Scope of evidence.} The compute-tier results of
\S\ref{sec:exp} use \emph{model tier} as the compute knob---a
realistic deployment lever, but not the only one. A complementary
within-model lever is the thinking-token budget on a fixed reasoning
model, which we study as a pilot in
Appendix~\ref{app:budget}: the budget knob controls realized thinking
length ($\rho{=}{+}0.78$), but pass rates on single-shot SWE-bench
patches saturate at every tested budget, so strategy separation is
within sampling noise at $N{=}30$. A careful within-model study at
larger $N$ or on a benchmark with more compute headroom is left to
future work.

\textbf{Construct validity of consequence.} Throughout the main text
the primary consequence label is produced by an LLM judge that sees
the gold patch. A three-annotator human study on a $150$-task
stratified sample drawn from both benchmarks
(Appendix~\ref{app:iaa}) shows the rubric to be moderately subjective
at the class-1-vs-class-2 boundary (inter-annotator
$\kappa = 0.380$ between the two naive annotators; Fleiss
$\kappa = 0.233$) but consistently interpreted within each annotator
(intra-annotator $\kappa$ at or near $1.0$). Crucially, the central
orthogonality result of \S\ref{sec:cons} replicates under the human
majority labels ($\rho = -0.066$, n.s.), and the LLM-with-patch judge
agrees with the human majority at $\kappa = 0.50$. We treat the human
labels as a transparency check on construct validity and retain the
LLM-with-patch label as the primary signal in the main results.

\textbf{Cross-domain transfer.} On Multi-SWE-bench mini, the
issue-only predictor's agreement with the with-patch reference
degrades to $\kappa = 0.38$ (full analysis in
Appendix~\ref{app:mswe}). Inspection shows that this benchmark's
task pool---CLI utilities, UI libraries, and parsers---is naturally
class-2-sparse, so the predictor has fewer high-consequence examples
to anchor on; the orthogonality result itself replicates on MSWE,
indicating that the construct transfers even when the predictor
needs domain-specific calibration. We expect a larger gain in
genuinely data-heavy domains (database operations, ORMs, financial
calculations) and plan a second domain study to confirm.

\textbf{Negative results.} Appendix~\ref{app:negative} records two
runs that did not produce the intended signal and are not used in the
main results: a within-model budget run on Qwen3-8B that saturated at
$\approx 2\%$ pass rate, and an initial Claude budget sweep that ran
into an API streaming constraint at the highest tested budget.

\textbf{Cost is a single scalar.} We assume $\mathrm{cost}(x)$ is
ordinal in $\{0, 1, 2\}$. Deployment-time cost is multidimensional
(financial, reputational, safety, regulatory) and is in general
context-dependent. The framework of Eq.~\eqref{eq:budgetobj} extends
naturally to richer cost vectors; the simple three-class instantiation
here is a deliberate starting point and is what current LLM judges
can reliably produce from issue text.

\section{Conclusion}

Adaptive test-time compute methods for reasoning models have, almost
uniformly, optimized accuracy under a budget. We argue that the
deployment objective is not accuracy but cost-weighted risk, and that
the missing signal---per-task consequence---is independently
measurable, distinct from difficulty and confidence, predictable from
issue text alone, and safely usable as a routing input. On a
16-model SWE-bench compute-tier benchmark, consequence-aware
allocation reduces cost-weighted loss by $30\%{+}$ over a
difficulty-aware baseline; a deployable predictor variant retains
over $90\%$ of that gain; and the difficulty baseline itself
underperforms random, because the hardest tasks are unsolvable at any
tier. Reasoning models do not need to be retrained to benefit from
this; the change is at the scheduling layer.

\bibliography{references}
\bibliographystyle{iclr2026_conference}

\appendix

\section{Human Annotation Study}
\label{app:iaa}

This appendix reports the human inter-annotator agreement (IAA) study
referenced in \S\ref{sec:cons} and \S\ref{sec:discussion}. The goal
is twofold: (i) establish whether the consequence rubric of
\S\ref{sec:cons} is interpretable by naive annotators, and (ii)
test whether the orthogonality result $\rho(\mathrm{cons},
\mathrm{difficulty}) \approx 0$ replicates when the consequence
label is provided by human majority rather than by an LLM judge.

\paragraph{Design.} We sampled $150$ unique tasks, stratified by the
LLM-with-patch consequence label (50 from each class), drawn $75$
from SWE-bench Lite and $75$ from Multi-SWE-bench mini. To measure
each annotator's self-consistency we inserted $15$ hidden duplicates
(five per class) so that each annotator's sheet contained $165$ rows
in a randomized order specific to that annotator. Three annotators
participated: one of the authors (who designed the rubric, denoted
$A$), one CS PhD outside the project (\emph{B}), and one
non-author CS graduate student (\emph{C}). All three saw only the
issue title and problem statement; they did not have access to the
gold patch, the file diff, or each other's labels. Each annotator
first worked through a $9$-task calibration warm-up with reference
labels before starting the $165$-task evaluation.

\paragraph{Inter-annotator agreement.} Table~\ref{tab:iaa} reports
pairwise Cohen's $\kappa$, three-rater Fleiss' $\kappa$, and
Krippendorff's $\alpha$ on the $150$ common unique tasks. Because
annotator $A$ designed the rubric, the strongest test of rubric
operability is the agreement between the two naive annotators,
$B \leftrightarrow C$, which lands at $\kappa = 0.380$ (fair
agreement on the Landis--Koch scale). The pooled three-rater statistics
(Fleiss $\kappa = 0.233$, Krippendorff $\alpha = 0.234$) are
correspondingly fair. This is moderate, not strong, agreement and
sits within the typical range reported for related ordinal
software-engineering rating tasks such as bug priority and code-review
severity.

\begin{table}[h]
\centering
\caption{\textbf{Inter-annotator and intra-annotator agreement} on the
$150$-task human study. Intra-annotator $\kappa$ is computed from the
$15$ hidden duplicates each annotator labeled. The main IAA indicator
is $B \leftrightarrow C$, the two naive annotators.}
\label{tab:iaa}
\small
\begin{tabular}{lcc}
\toprule
Comparison & Cohen's $\kappa$ & Raw agreement \\
\midrule
\multicolumn{3}{l}{\textit{Inter-annotator (on the 150 unique tasks)}} \\
$A \leftrightarrow B$  (rubric designer vs CS PhD)        & $0.151$ & $47.3\%$ \\
$A \leftrightarrow C$  (rubric designer vs graduate student) & $0.199$ & $45.3\%$ \\
$\mathbf{B \leftrightarrow C}$  \textbf{(two naive annotators; main IAA)} & $\mathbf{0.380}$ & $\mathbf{58.0\%}$ \\
Fleiss' $\kappa$ (3 raters)                                & $0.233$ & --- \\
Krippendorff's $\alpha$ (nominal)                          & $0.234$ & --- \\
\midrule
\multicolumn{3}{l}{\textit{Intra-annotator (from 15 hidden duplicates each)}} \\
$A$ self-consistency                                       & $1.000$ & $100\%$ \\
$B$ self-consistency                                       & $1.000$ & $100\%$ \\
$C$ self-consistency                                       & $0.789$ & $86.7\%$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Intra-annotator agreement.} The intra-annotator block of
Table~\ref{tab:iaa} reports each annotator's self-consistency on the
$15$ duplicate tasks. Two annotators are perfectly self-consistent
($\kappa = 1.0$, $100\%$ raw agreement); the third reaches $\kappa = 0.789$
($86.7\%$). \emph{Each annotator has a stable internal standard; the
moderate inter-annotator agreement therefore reflects rubric ambiguity
across people, not noise within any one annotator.}

\paragraph{Disagreement topology.} Of the inter-annotator disagreements,
the overwhelming majority are adjacent-class confusions
($0 \leftrightarrow 1$ or $1 \leftrightarrow 2$), not severe
$0 \leftrightarrow 2$ flips. For the main $B \leftrightarrow C$ pair,
$58.0\%$ of items agree, $37.3\%$ disagree by one class, and only
$4.7\%$ disagree by two classes; the other two pairs follow the same
pattern ($\sim 47\%$ adjacent vs $5$--$7\%$ severe). The rubric's
class boundaries---particularly between class $1$ (locally recoverable
functional bug) and class $2$ (silent corruption or
irreversibility)---are the dominant source of disagreement, not
fundamental directional confusion between low- and high-stakes.

\paragraph{Final human labels and class distribution.} We resolve each
of the $150$ tasks by majority vote of the three annotators. $142$
tasks have a clear majority ($\geq 2$ of $3$ votes); the remaining
$8$ are three-way splits ($0/1/2$) and are conservatively assigned to
the middle class. The resulting class distribution is
$\{0{:}45,\ 1{:}67,\ 2{:}38\}$.

\paragraph{Re-running orthogonality under human labels.} On the $75$
SWE-bench-Lite tasks for which we have both human majority labels and
the difficulty estimate of \S\ref{sec:cons},
\[
\rho\bigl(\mathrm{human}\!-\!\mathrm{cons},\ \mathrm{difficulty}\bigr) = -0.066,\quad p = 0.57.
\]
The central orthogonality result of \S\ref{sec:cons} \textbf{replicates}
under human labeling. Consequence remains statistically independent
of difficulty whether the label is produced by a rule, by an LLM
shown the gold patch, by an LLM shown only the issue, or by a human
majority of three independent annotators.

\paragraph{Agreement with the existing labelers.} Table~\ref{tab:human-vs-llm}
compares the human majority label against the three labeling pipelines
used in the main text. The LLM-with-patch judge agrees with the human
majority at $\kappa = 0.50$ and the issue-only predictor at
$\kappa = 0.52$---both moderate agreement and close to the
$B \leftrightarrow C$ ceiling of $0.38$. The rule-based labeler agrees
at $\kappa = 0.22$, reflecting its purely lexical construction.

\begin{table}[h]
\centering
\caption{Cohen's $\kappa$ between the human majority label and each of
the labeling pipelines used in the main text, on the $75$ SWE-bench
Lite tasks of this study.}
\label{tab:human-vs-llm}
\small
\begin{tabular}{lc}
\toprule
Pipeline & $\kappa$ vs human majority \\
\midrule
Rule-based labeler                       & $0.223$ \\
LLM-with-patch judge (Qwen3-8B)          & $0.500$ \\
LLM-issue-only predictor (Qwen3-8B)      & $0.517$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Honest interpretation.} The IAA evidence supports a
\emph{nuanced} reading of consequence as a construct. Within an
individual annotator, the rubric is interpretable and applied
consistently (intra-$\kappa$ near $1.0$). Across naive annotators it
is moderately subjective at the class-1-vs-class-2 boundary
(inter-$\kappa = 0.38$). Crucially for the paper's claims, however,
the agreement is high enough that the LLM-with-patch judge agrees with
the human majority at $\kappa = 0.50$, and the central orthogonality
result of \S\ref{sec:cons} replicates under the human labels with
$\rho = -0.066$. We therefore retain the LLM-with-patch label as the
primary consequence signal throughout the main text while presenting
the human evidence here as a transparency check on its construct
validity.

\section{Rubric Ablation: All Four Labelings Give the Same Conclusion}
\label{app:rubric}

A natural reviewer concern is that the central results of the paper
might be artefacts of the specific labeling pipeline used in the main
text (LLM-with-patch). This appendix re-runs the two headline
analyses---F1 orthogonality (\S\ref{sec:cons}) and the matched-compute
allocation gain (\S\ref{sec:exp})---separately under each of the four
labelings of the rubric, and shows the conclusions are stable.

\textbf{F1 orthogonality replicates under every labeling.}
Table~\ref{tab:abl-ortho} reports $\rho(\mathrm{consequence}, \mathrm{difficulty})$
under the four labeling pipelines. None of the four correlations
reaches significance at $\alpha = 0.05$, and all four are within
$\pm 0.11$ of zero. The construct is statistically independent of
difficulty regardless of who or what produces the label.

\begin{table}[h]
\centering
\caption{\textbf{Rubric ablation — orthogonality.} Spearman
$\rho(\mathrm{consequence}, \mathrm{difficulty})$ on SWE-bench Lite
under each of the four labelings.}
\label{tab:abl-ortho}
\small
\begin{tabular}{lccc}
\toprule
Labeling pipeline & $n$ & $\rho$ & $p$ \\
\midrule
Rule-based                                  & $300$ & $+0.047$ & $0.42$ \\
LLM-with-patch  (Qwen3-8B; paper primary)   & $300$ & $-0.096$ & $0.10$ \\
LLM-issue-only  (Qwen3-8B predictor)        & $300$ & $-0.109$ & $0.06$ \\
Human majority  (3-annotator study)         & $\phantom{0}75$  & $-0.066$ & $0.57$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Allocation gain replicates under every labeling.}
Table~\ref{tab:abl-gain} re-runs the matched-compute experiment of
\S\ref{sec:exp} four times: in each row, the indicated labeling is
substituted both as the oracle routing signal and as the weight
$\mathrm{cons}(x)$ used in computing the cost-weighted loss. The
priority-aware oracle beats the difficulty-aware baseline by between
$+30.8\%$ and $+36.3\%$ across all four labelings, with the human
labels giving the largest improvement on the 75-task subset they
cover. The paper's main claim (``a $30\%+$ reduction in cost-weighted
loss'') is therefore not a property of the LLM-with-patch labeler
specifically: it is a property of the rubric.

\begin{table}[h]
\centering
\caption{\textbf{Rubric ablation — allocation gain.} Cost-weighted loss
$\mathcal{L}$ at the matched-compute operating point (top-$25\%$ to
the premium tier) on the 16-model SWE-bench compute-tier benchmark,
recomputed four times---once per labeling pipeline---using that
labeling both as the oracle routing signal and as the consequence
weight in $\mathcal{L}$. ``$\Delta$ vs Diff'' is the relative
reduction in $\mathcal{L}$ achieved by the priority-aware oracle
relative to the difficulty-aware (Snell-style) baseline within the
same labeling.}
\label{tab:abl-gain}
\small
\begin{tabular}{lrrrrrr}
\toprule
Oracle labeling & $n$ & Diff & Random & Cons-oracle & Priority-oracle & $\Delta$ vs Diff \\
\midrule
Rule-based                                & $300$ & $406.2$ & $352.5$ & $330.8$ & $\mathbf{281.2}$ & $+30.8\%$ \\
LLM-with-patch \emph{(paper main)}        & $300$ & $268.2$ & $233.8$ & $205.8$ & $\mathbf{179.8}$ & $+33.0\%$ \\
LLM-issue-only \emph{(predictor)}         & $300$ & $307.5$ & $266.2$ & $229.5$ & $\mathbf{201.5}$ & $+34.5\%$ \\
Human majority \emph{(75 SWE tasks)}      & $\phantom{0}75$  & $\phantom{0}75.8$  & $\phantom{0}67.5$  & $\phantom{0}55.2$  & $\phantom{00}\mathbf{48.2}$  & $+36.3\%$ \\
\bottomrule
\end{tabular}
\end{table}

The fact that the rule-based labeler---which is just a hand-written
lexical pattern matcher---and the human-majority labeler---which never
sees an LLM---both yield $30\%+$ improvements rules out the
``the gain is something the LLM judge invented'' interpretation. The
remainder of the paper retains the LLM-with-patch label as the
primary signal in the main results because it is the labeling under
which all four supporting analyses
(\S\ref{sec:cons}, \S\ref{sec:pred}, \S\ref{sec:exp}, \S\ref{sec:whyfail})
are jointly applicable on the full 300-task pool.

\section{Predictor Calibration Details}
\label{app:predictor}

This appendix expands \S\ref{sec:pred} with full per-class metrics and
confusion matrices for both predictors evaluated against the
LLM-with-patch reference on all $N{=}300$ SWE-bench Lite tasks. The
class distribution of the with-patch reference is
$\{0{:}60,\ 1{:}196,\ 2{:}44\}$.

\textbf{Primary predictor: Qwen3-8B issue-only.} Cohen's
$\kappa = 0.572$; class-2 recall $= 88.6\%$ ($39/44$). The confusion
matrix is:
\[
\begin{array}{c|ccc|c}
& \hat{c}{=}0 & \hat{c}{=}1 & \hat{c}{=}2 & \text{total} \\\hline
c{=}0 & 47  & 13  & 0  & 60  \\
c{=}1 & 11  & 140 & 45 & 196 \\
c{=}2 & 0   & 5   & 39 & 44  \\\hline
\text{total} & 58 & 158 & 84 & 300
\end{array}
\]
Per-class precision/recall/F1: class~0 ($0.81/0.78/0.80$), class~1
($0.89/0.71/0.79$), class~2 ($0.46/0.89/0.61$). The Qwen predictor
hits zero severe errors in \emph{both} directions: $[2,0]=0$
(no high-stakes task missed as low-stakes) and $[0,2]=0$
(no low-stakes task escalated to high-stakes), giving an overall
severe-error rate of $0/300 = 0.0\%$.

\textbf{Cross-model robustness predictor: Claude Sonnet~4.5
issue-only.} Cohen's $\kappa = 0.379$; class-2 recall $= 52.3\%$
($23/44$). The confusion matrix is:
\[
\begin{array}{c|ccc|c}
& \hat{c}{=}0 & \hat{c}{=}1 & \hat{c}{=}2 & \text{total} \\\hline
c{=}0 & 33 & 25 & 2 & 60 \\
c{=}1 & 25 & 148 & 23 & 196 \\
c{=}2 & 0 & 21 & 23 & 44 \\\hline
\text{total} & 58 & 194 & 48 & 300
\end{array}
\]
Per-class precision/recall/F1: class~0 ($0.57/0.55/0.56$), class~1
($0.76/0.76/0.76$), class~2 ($0.48/0.52/0.50$). Despite a noticeably
lower aggregate $\kappa$ than the within-model predictor, the
class-2$\to$class-0 cell still holds at zero, and total severe
errors in \emph{either} direction sum to $2/300 = 0.7\%$, supporting
the deployment-safety claim across model families.

\section{Per-Task Marginal Utility (Excerpt)}
\label{app:marginal}

This appendix supports \S\ref{sec:marginal} with a per-task view of
the priority signal
$\mathrm{priority}(x) = \mathrm{cons}(x) \cdot
\Delta\mathrm{success}(x)$. The complete per-task table (all $300$
rows) is included with the submission as a supplementary CSV;
Table~\ref{tab:marg} below shows the top eight tasks by priority and
the top four tasks by raw difficulty for contrast.

\begin{table}[h]
\centering
\caption{Per-task marginal utility excerpt. Top eight tasks by
priority $= \mathrm{cons}\cdot\Delta\mathrm{success}$ and top four
tasks by difficulty $= 1 - \overline{\mathrm{passrate}}$. The
priority-ranked tasks all have moderate difficulty and large
$\Delta\mathrm{success}$; the difficulty-ranked tasks all have
$\Delta\mathrm{success} \approx 0$ (premium tier cannot solve them
either), so allocating extra compute to them returns zero
cost-weighted gain.}
\label{tab:marg}
\small
\begin{tabular}{lcccc}
\toprule
Selection & cons & difficulty & $\Delta$success & priority \\
\midrule
\multicolumn{5}{l}{\textit{Top-8 by priority}} \\
top-1 & $2$ & $0.50$ & $1.00$ & $2.00$ \\
top-2 & $2$ & $0.50$ & $1.00$ & $2.00$ \\
top-3 & $2$ & $0.56$ & $0.75$ & $1.50$ \\
top-4 & $2$ & $0.56$ & $0.75$ & $1.50$ \\
top-5 & $2$ & $0.62$ & $0.75$ & $1.50$ \\
top-6 & $2$ & $0.62$ & $0.75$ & $1.50$ \\
top-7 & $1$ & $0.31$ & $1.00$ & $1.00$ \\
top-8 & $1$ & $0.31$ & $1.00$ & $1.00$ \\
\midrule
\multicolumn{5}{l}{\textit{Top-4 by difficulty}} \\
top-1 & $1$ & $1.00$ & $0.00$ & $0.00$ \\
top-2 & $1$ & $1.00$ & $0.00$ & $0.00$ \\
top-3 & $2$ & $1.00$ & $0.00$ & $0.00$ \\
top-4 & $0$ & $1.00$ & $0.00$ & $0.00$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Real Thinking-Budget Control: Pilot}
\label{app:budget}

The main-text experiments allocate compute by routing tasks across
\emph{model tiers}. A complementary lever is the
\emph{thinking-token budget} on a single fixed model. We report a
pilot study on Claude Sonnet~4.5 across five extended-thinking
budgets $\{1024, 2048, 4096, 8192, 16000\}$ on a 30-task stratified
sub-sample of SWE-bench Lite, evaluated by an LLM-judge for patch
correctness.

The within-budget control knob works as intended---the rank
correlation between requested budget and realized thinking-character
output is $\rho = +0.78$ ($p < 10^{-10}$). However, the resulting
pass rates are flat across all five budgets ($10\%$--$17\%$), and the
separation between Uniform, Difficulty-aware, Consequence-aware
(predictor), and Priority-aware allocation strategies is within
sampling noise at this sample size. We attribute the saturation to
the difficulty of single-shot SWE-bench patches at Claude's strength
level: at every budget tested, most class-2 tasks remain unsolvable
without an agent scaffold or multi-turn retrieval. We therefore treat
this run as a pilot/limitation rather than a primary result; a
careful within-model study at larger $N$ and with iterative
patching---or on benchmarks where the budget knob has more
headroom---is left to future work.

\section{Cross-Domain Transfer: Multi-SWE-bench mini}
\label{app:mswe}

We evaluate transfer of the consequence labeling and predictor to a
second software-engineering benchmark, Multi-SWE-bench
mini~\citep{ma2024multiswebench}, which covers non-Python
ecosystems (Rust, JS, Go, etc.) and is dominated by CLI utilities,
parsers, and UI libraries.

\textbf{Label distribution.} Of the $400$ MSWE tasks the
LLM-with-patch judge places only $3$ in class~2 (vs.\ $44/300$ on
SWE-bench Lite), reflecting that this benchmark's task pool is
naturally class-2-sparse: CLI / parser / UI bugs rarely fall in the
silent-corruption or irreversible-operation regime that defines
class~2.

\textbf{Predictor agreement.} On MSWE, the issue-only Qwen predictor
agrees with the with-patch reference at Cohen's $\kappa = 0.38$, down
from $0.572$ on SWE-bench Lite. We interpret this drop primarily as
a base-rate effect: with so few class-2 examples to anchor on, the
predictor's class-2 calibration is weaker. The orthogonality result
$\rho(\mathrm{cons}, \mathrm{difficulty}) \approx 0$ replicates on
MSWE under both labelings, indicating the construct itself
transfers; only the predictor needs domain-specific calibration when
the task pool's class distribution shifts.

\textbf{Implication.} We expect a domain with a more balanced
class-2 base rate (e.g., database migrations, ORM/SQL, financial
calculations) to yield a stronger predictor; a follow-up study in
such a domain is planned.

\section{Negative and Superseded Experiments}
\label{app:negative}

For completeness we record two experiments that did not produce the
intended signal and are not used in the main results.

\textbf{P0a: within-model budget on Qwen3-8B.} An earlier
within-model variant of the budget-control study used Qwen3-8B as
the reasoning model across four max-token budgets
$\{1024, 4096, 16000, 32000\}$ on a 50-task stratified sub-sample.
The pass rate was $\approx 2\%$ at every budget tested, indicating
that single-shot SWE-bench patches are below the model's capability
threshold without an agent scaffold. The experiment was superseded
by the Claude-based pilot of Appendix~\ref{app:budget}.

\textbf{P0d: Claude budget sweep with non-streaming API.} An initial
Claude budget sweep at thinking budgets
$\{1024, 4096, 16000, 32000\}$ encountered an Anthropic API constraint
in non-streaming mode: every request at the $32000$-token budget
returned a streaming-required error, leaving the highest-budget cell
of the design empty. The remaining three budgets had $n=10$ tasks per
$(\text{budget}, \text{class})$ cell---too noisy to separate
strategies. The run was replaced by the streaming-based five-budget
pilot of Appendix~\ref{app:budget}.

\end{document}
